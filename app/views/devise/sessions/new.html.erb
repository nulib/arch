<h2>Log in</h2>

<%= form_for resource, as: resource_name, url: session_path(resource_name), html: { class: 'col-
md-3' } do |f| %>
  <div class="form-group">
    <label for="user_username">Northwestern Net ID:</label>
    <%= f.text_field :username, autofocus: true, class: 'form-control' %>
  </div>

  <div class="form-group">
    <%= f.label :password %><br />
    <%= f.password_field :password, autocomplete: "off", class: 'form-control' %>
  </div>

  <div class="actions">
    <%= f.submit "Log in", class: "btn btn-primary" %>
  </div>
<% end %>
